<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="pageLoaded" class="page"
  xmlns:ui="nativescript-twilio">
  <StackLayout class="p-20">
    <StackLayout>
      <TextField
        text="{{ accessTokenUrl }}"
        hint="URL for getting a Twilio Access Token"
      />
      <TextField
        text="{{ authorizationHeader }}"
        hint="Value for authorization header"
      />
      <TextField
        text="{{ phoneNumber }}"
        hint="Phone number (i.e +34606049850)"
      />
    </StackLayout>
    <StackLayout orientation="horizontal">
      <TextField
        text="{{ option1.key }}"
        hint="option1.key"
      />
      <TextField
        text="{{ option1.value }}"
        hint="option1.value"
      />
    </StackLayout>
    <StackLayout orientation="horizontal">
      <TextField
        text="{{ option2.key }}"
        hint="Enter option2.key"
      />
      <TextField
        text="{{ option2.value }}"
        hint="Enter option2.value"
      />
    </StackLayout>
    <Button class="bg-primary call-button" text="CALL" tap="{{ onCall }}" />
  </StackLayout>
</Page>